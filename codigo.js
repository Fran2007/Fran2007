const button = document.querySelector('.buton');
const contenedor1 = document.querySelector('.Contenedor-1');
const contenedor2 = document.querySelector('.Contenedor-2');

//button.addEventListener('mouseenter', (e)=>{
      // alert('Di click en un boton')
//});

//contenedor1.addEventListener('keypress', (e)=>{
     // alert('Di click en el contenedor ')
       //e.stopPropagation();
//},true);

//contenedor2.addEventListener('mouseleave',(e)=>{
      // alert('Di click en el contenedor azul')
//})